---
import { Image } from "astro:assets";

const { id, image, title } = Astro.props;
---

<section id={id} class="season-hero">
  <div class="season-hero-inner">

    {image && (
      <Image
        src={image}
        alt=""
        class="season-hero-icon"
        priority
      />
    )}

    <h2 class="season-hero-title">
      {title}
    </h2>

    <p class="season-hero-intro">
      Le mariage se déroulera en août, en pleine saison sèche à l'île Maurice.
      Des journées ensoleillées, des températures douces et un ciel d'un bleu profond —
      la période idéale pour célébrer et profiter de l'île en famille.
    </p>

  </div>

  <div class="scroll-indicator">↓</div>
</section>

<style>
  .season-hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding-inline: clamp(1.5rem, 8vw, 5rem);
  }

  .season-hero-inner {
    max-width: 900px;
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    z-index: 1;
  }

  .season-hero-icon {
    display: block;
    height: clamp(100px, 20vw, 200px);
    width: auto;
    margin-bottom: var(--space-6);
  }

  .season-hero-title {
    font-size: clamp(2.4rem, 6vw, 4.8rem);
    margin-bottom: var(--space-4);
  }

  .season-hero-intro {
    font-size: var(--fs-body-lg);
    line-height: var(--lh-normal);
    max-width: 65ch;
    margin: 0 auto;
  }

  .scroll-indicator {
    position: absolute;
    bottom: clamp(20px, 4vw, 40px);
    left: 50%;
    transform: translateX(-50%);
    font-size: clamp(1.2rem, 3vw, 1.6rem);
    opacity: 0.6;
    animation: bounce 2s infinite;
  }

  @keyframes bounce {
    0%, 100% { transform: translate(-50%, 0); }
    50%       { transform: translate(-50%, 10px); }
  }

  @media (prefers-reduced-motion: reduce) {
    .scroll-indicator {
      animation: none;
      opacity: 0.4;
    }
  }
</style>
