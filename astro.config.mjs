// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://OpusEnigma.github.io',
  base: '/nuptiae/',   // Le nom du repo
  devToolbar: {
    enabled: false,
  },
});
